name: Publish common package to Github packages
on:
  push:
    paths:
      - 'common/**'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v3
        with:
          java-version: "17"
          distribution: "adopt"

      - name: Publish package
        run: mvn deploy -f common/pom.xml
        env:
          GITHUB_TOKEN: ${{ secrets.COMMON_SECRET }}